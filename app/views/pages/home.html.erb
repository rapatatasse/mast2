<h1>Pages#home</h1>
<p>Find me in app/views/pages/home.html.erb</p>

<code>A faire exo dans le ficheir todo.txt</code>


<div data-controller="hello">
  <input data-hello-target="name" type="text">
  <button data-action="click->hello#greet">greet</button>
  <div data-hello-target="result"></div>
</div>


<%= link_to "Pages clients", clients_path, class: "btn btn-primary" %>
<br>
Mes clients (<%= @clientsnb %>)
<div class="card">
  <div class="card-body">
    <h5 class="card-title">utilisateur actif</h5>
    <p class="card-text"><%= current_user.email if user_signed_in? %></p>
    <% if user_signed_in? %>
      <%@clients.each do |client| %>
        <p><%= client.nom %></p>
      <%end%>
    <%end%>
  </div>
</div>